```
# init 
git init

# add-commit-push
git add .
git commit -m "update"
git push

# create and change branch
git branch -b dev
git checkout dev

# another way to create and change branch
git swich -c dev
git swich dev

# merge other brance
git checkout master
git merge dev
git push
```